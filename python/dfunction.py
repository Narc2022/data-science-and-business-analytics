course = "Python Programming"

# used length function
print(len(course))

# accessing index
print(course[0]);

# last index on string
print(course[-1]);

# from index:index
print(course[0:3]);

# from index to last
print(course[0:])

# copy of string
print(course[:])
